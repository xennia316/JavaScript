/**
 * by @xennia316
 */
let processed = 0;
//processed initialized to zero
function processArg(num) {
  return (num + 3) / 5;
}
// takes number and adds three then divides by 5.
processed = processArg(7);

console.log(processed);