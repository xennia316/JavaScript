/**
 * There are several operators in  js: + , - , * , / .
 */
 let FIRST = 3 + 1;
 const myVar = 2 * 1;
 const aVar = 6 - 2;
 let NEW = 2 / 1;

 console.log(FIRST);
 console.warn("Stop right there", myVar);
 console.clear();
 console.timeStamp(String);